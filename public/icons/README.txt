/**
 * Icon placeholders.
 *
 * Real PNG icons (192/512/maskable/apple-touch) live in this folder. They are
 * referenced by `/public/manifest.webmanifest` and the metadata in
 * `src/app/layout.tsx`.
 *
 * Generate production-ready icons with `pnpm run icons:generate` (script in
 * /scripts/generate-icons.ts — uses a single source SVG and emits every size).
 *
 * Required files:
 *   - icon-192.png        (192×192, any purpose)
 *   - icon-512.png        (512×512, any purpose)
 *   - maskable-192.png    (192×192, maskable purpose)
 *   - maskable-512.png    (512×512, maskable purpose)
 *   - apple-icon-180.png  (180×180, iOS home screen)
 *   - favicon.ico         (multi-resolution favicon)
 */
